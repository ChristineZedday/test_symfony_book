{% extends 'base.html.twig' %}

{% block title %}Bibliotheque{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>La liste de nos ouvrages:</h1>

  <table>
  <tr>
  <th>Titre</th>
  <th>Auteur</th>
  <th>Date de parution</th>
  <th>Numéro ISBN13</th>
  <th>URL</th>
  <th>Actions</th>
  </tr>
   {% for book in books %}
  <tr>
  <td>{{book.title}}</td>
  <td>{{book.author}}</td>
  <td>{{book.getDateStr}}</td>
  <td>{{book.isbn13}}</td>
  <td>{{book.url}}</td>
  <td><a href="{{path('book_show', {'id': book.id})}}">Voir le résumé</a><br/>
 <a href="{{path('book_edit', {'id': book.id})}}">Modifier</a>
 {{include('book/_form_delete.html.twig')}}</td>
  </tr>
  {% endfor %}
  </table>
   <p> <a href="{{path('book_new')}}">Ajouter un livre</a></td></p>
</div>


{% endblock %}
